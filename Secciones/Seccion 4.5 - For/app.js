/*
imprimir la tabla del 10 y ponerle limite a la tabla
*/
 

let base = 10;
let limite = 20;

for (let i = 1; i <= limite;  i++) {

    let resultado = base * i;
   
    console.log(base + ' x ' + i +'= ' + resultado);
}
